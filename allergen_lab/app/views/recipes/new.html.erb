<h1>Create a new recipe:</h1>
<%= form_for @recipe do |f|%>
<%= f.label :name%>
<%= f.text_field :name%>
<%=f.collection_select :user_id, User.all, :id, :name%>

<%=f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name%>

<%=f.submit "Submit Recipe or Whatevas"%>
<%end%>

